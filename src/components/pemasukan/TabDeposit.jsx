import React from 'react';
import { DepositPage } from '@/components/deposit/DepositPage';

/**
 * TabDeposit — Deposit tab within the income dashboard.
 *
 * Renders the existing DepositPage component inline,
 * providing deposit management functionality.
 */
export function TabDeposit() {
  return <DepositPage />;
}
